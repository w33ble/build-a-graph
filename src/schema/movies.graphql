type Movie {
  id: String!
  type: String!
  title: String!
  year: Int
  runtimeMinutes: Int
  genres: [String]!
  people: [Person]
}

extend type Query {
  movies(id: String, title: String, year: Int, limit: Int, skip: Int): [Movie]
}
