type Person {
  id: String!
  name: String!
  birthYear: Int
  deathYear: Int
  professions: [String]
  knownFor(limit: Int, skip: Int): [Movie]
}

extend type Query {
  people(id: String, name: String, birthYear: Int, deathYear: Int, limit: Int, skip: Int): [Person]
}
